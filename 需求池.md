# 需求池

这个文件用于把用户语言转成可执行的产品与技术判断。原则是：不无谓增加业务量，但保证网站设计能满足真实业务需求。

## 工作原则

- 先理解业务目的，再决定技术实现。
- 每个需求都保留用户原话，避免过度翻译导致失真。
- 优先做能直接改善转化、效率、信任感或交付质量的事情。
- 不为了“功能完整”堆功能；复杂度必须服务于业务结果。
- 设计判断要克制、清晰、有取舍，像产品负责人一样对体验负责。

## 发布与测试规则

- 每次实现需求后，必须先在 dev 环境测试。
- 每次改完代码后，必须同步更新对应测试流程：说明要测哪个页面、哪个入口、哪些操作、预期结果是什么。
- 如果我无法完整执行测试，必须明确提示你需要测试哪些关键路径。
- 只有你确认 “OK” 之后，才可以提交到 GitHub，并更新到 testing service。
- 没有你的 OK，不主动 push、不创建 PR、不触发 testing service 更新。

## 需求池

| ID | 用户原话 | 业务目标 | 建议方案 | 优先级 | 状态 | 下一步 |
| --- | --- | --- | --- | --- | --- | --- |
| R-001 | “帮我维护一个 需求池.md。我会用用户的语言跟你交流，你理解我的自然语言并且帮我在业务和技术之间取得最佳平衡。不要把无畏增加业务量，但是要保证网站的设计是可以满足我的业务需求的。你要把自己想象成 steve jobs。我们这个对话就只有这一个目的，明白？” | 建立一个长期需求管理机制，把自然语言需求转成产品、设计、技术上的可执行判断。 | 创建本文件，后续每次收到需求时补充：用户原话、业务目标、建议方案、优先级、状态和下一步。 | 高 | 已完成 | 后续直接发送需求，我会更新本文件。 |
| R-002 | “All the trials email need to have the start date and end date. And then the time of arrival of the trial.” | 让 trial 邮件包含完整到访信息，减少家长和运营人员来回确认。 | 所有 trial 相关邮件统一加入 trial start date、end date、arrival time。用环境变量集中配置，不新增复杂后台管理。 | 高 | 已实现，待测试 OK | 需要在 testing/dev 环境设置真实 trial 日期与 arrival time，并验证邮件日志内容。 |
| R-004 | “The save application does not need to send the email to the user.” | 保存申请应是内部/草稿动作，不打扰用户，避免误以为申请已正式提交。 | Save trial application 只保存数据并通知 admin，不再给用户发送 application received 邮件。正式信息检查/资格结果邮件仍保留。 | 高 | 已实现，待测试 OK | 在有数据库的环境提交 trial application，确认用户没有收到/生成 application received 邮件。 |
| R-005 | “If the application has the membership code in the backend table. Then just need to replace the existing membership information. No need to add the new record.” 补充：“第三点，你只需要覆盖birthday和birthday cert的file就可以了，其他不需要改动目前看。” 再补充：“需要update，帮我改一下和birthday联动相关的都需要update。” 反馈：“同一个membership code，修改了birthday，expect的是在table只显示一条record。” 再确认：“membership需要是unique的。” | 避免同一会员和同一 club invite application 重复记录，同时保持 birthday 与派生信息一致，避免误覆盖其它会员资料。 | 数据库层面要求 ClubInviteApplication.membershipCode 唯一；如果 membership code 已存在，只更新现有 Player 的 birthday 和 ageGroup，并 upsert 已有 ClubInviteApplication，不新增 application record；birth cert 通过新上传的最新 Document 关联到同一 Player。前端 state 也按 id/membership code 去重替换，避免不刷新页面时显示两条。其他会员字段保持不变。migration 会先清理旧重复记录，只保留最新一条。 | 高 | 已实现，待测试 OK | 用已有 membership code 提交 club invite trial，确认 Admin table 只显示一条记录，数据库拒绝重复 membershipCode，Player 未重复创建，且 birthday、ageGroup 与最新 birth cert 已更新。 |
| R-006 | “Reminder of the ago group content need to be highlighted.” | 让年龄组提醒更容易被用户看到，减少选错年龄组或忽略规则。 | 在 Date of Birth 字段下方突出 age group reminder，但不使用红色 error 风格；应使用中性提示色或信息提示色，例如 amber、blue-gray、soft yellow。 | 中 | 已实现，待测试 OK | 在 dev 环境确认 reminder 足够醒目，但不再像 error message。 |
| R-007 | “admin ui的Club Invite Applications和New Trial Applications的table需要把player的birthday 展示出来才可以” | Admin 核查申请时需要直接看到球员生日，便于和 birth certificate、age group 交叉验证。 | 在 Club Invite Applications 和 New Trial Applications table 中新增 Birthday 列，使用现有 dateOfBirth 数据展示，不新增数据字段。 | 高 | 已实现，待测试 OK | 用户登录 admin 后确认两张表都展示 Birthday。 |
| R-008 | “1，把Date of Birth下的Select a date of birth to calculate the 2026 age group.这个提示的样式highlight。2，birthday of date的date 组建需要disbale掉Outside Trial Age Groups的日期。3，disbled的input框需要highlight样式。” | 降低生日/年龄组选错的概率，让用户明确知道哪些日期无效，以及哪些字段是系统锁定不可编辑。 | 高亮 Date of Birth 下的 age group 计算提示；Date of Birth 控件限制可选范围，禁用会落入 Outside Trial Age Groups 的日期；统一 disabled input/select 的视觉样式，让锁定字段明显但不干扰填写。 | 高 | 已实现，待测试 OK | 在 dev 环境确认 DOB 只允许有效 trial 年龄范围、提示足够醒目、club invite locked 字段样式清楚。 |
| R-009 | “email的tmp需要改成富文本instead of只有一个textare” | Admin 编辑邮件时需要更接近真实邮件内容，提升可读性和正式感，减少纯文本模板的表达限制。 | 将 admin email composer 从纯 textarea 升级为轻量富文本编辑。建议先支持基础格式：段落、换行、加粗、列表、链接和预览/发送兼容；不做复杂拖拽邮件设计器，避免过度建设。 | 中 | 已实现，待测试 OK | 在 dev 环境打开任意 admin 邮件动作，确认可编辑富文本、加粗/列表/链接可用，发送后 email log 正常记录。 |
| R-010 | “reminder不应该是红色，红色的看起来更像是个error msg” | 避免用户把 reminder 误解为错误提示，保持表单视觉语义清晰。 | 将 reminder / hint highlight 从红色改为非错误语义颜色。推荐用 amber/yellow 作为注意提醒，或 blue-gray 作为信息提示；error 才保留红色。 | 中 | 已实现，待测试 OK | 在 dev 环境确认 DOB reminder 使用 amber/yellow 提示色，disabled locked 字段使用 blue-gray，不再像 error message。 |
| R-011 | “每次改完code的时候你需要顺便同步一下测试流程” | 保证每次实现后都有清晰验收路径，减少你测试时不知道该点哪里、看什么结果。 | 每次代码变更后，同步更新测试流程说明，至少包含页面入口、操作步骤、预期结果和需要你人工确认的点。 | 高 | 已完成 | 后续每次代码改动都按此规则执行。 |
| R-012 | “根据 Consumer Report，看下需求池和业务流程测试还有什么需要 update 的。” | 从真实家长视角检查报名体验，优先减少困惑、减少人工支持、提高报名转化。 | 根据 Consumer Report 新增消费者体验差距评估和测试缺口；优先处理年龄不匹配提示、trial 剩余名额、onboarding code 邮件说明、电子 debit order、OTP 帮助和移动端体验。 | 高 | 已记录，待拆分实现 | 按 Consumer Report 优先级进入后续开发计划；每个实现都要同步业务流程测试。 |
| R-013 | “First Touch / Little Warriors age mismatch error too vague.” | 家长选错 programme 或孩子年龄不符合时，需要知道为什么错、应该选哪个、如何联系 club。 | 年龄校验错误显示孩子当前年龄、First Touch / Little Warriors 允许年龄范围、建议正确 programme；如果超出范围但可能适合 Academy trial，给出 contact club 引导。 | 高 | 已实现，待测试 OK | 用 trial code 进入 onboarding，选择不匹配年龄的 First Touch / Little Warriors，确认错误说明包含年龄、允许范围、建议路径和 support email。 |
| R-014 | “Trial registration should show remaining spots.” | 让家长知道 trial 名额是否紧张，减少付款前的不确定感。 | Trial registration 显示 capacity 与 remaining spots，例如 “50 of 100 spots remaining”。需要从 TrialWindow / TrialEvent 或当前 trial capacity 配置读取，不建议写死。 | 高 | 已实现，待测试 OK | 当前 MVP 使用 `trialWindows.ts` 配置作为 capacity 数据源；后续正式 TrialWindow 后台上线后替换为 API。 |
| R-015 | “Onboarding code email needs clear next steps.” | 用户拿到 onboarding code 后必须知道下一步在哪里输入、怎么完成注册和付款。 | Onboarding code 邮件加入 4 步说明：登录账号、进入 Academy Programmes、点击 Enter Onboarding Code、完成 registration/payment。 | 高 | 已实现，待测试 OK | Admin successful review email composer 和后端 email log 都应包含 Next steps。 |
| R-016 | “Debit order print/sign/upload is poor mobile UX.” | 避免手机用户被打印签字上传流程卡住，提升付款/入会完成率。 | 电子 debit order mandate 作为默认路径；打印签字上传只作为 fallback。可先设计接口和 UI 状态，再决定 PayFast Debit Order / Ozow 等 provider。 | 高 | 待实现 | 评估 payment provider 能力，定义 mandate record 和 fallback flow。 |
| R-017 | “Kit/sock size guide link near selection boxes.” | 家长选择尺码时需要即时参考，减少尺码错误和后续更换成本。 | 在 kit size / sock size 字段旁增加 size guide 链接或弹窗；不做复杂尺码推荐，先提供清晰对照表。 | 中 | 待实现 | 等 onboarding required fields 补齐时一起实现。 |
| R-018 | “OTP screen needs resend/spam/contact admin help.” | OTP 是登录入口，失败时必须有自助恢复路径，减少 support 压力。 | OTP 页面提供 resend、检查 spam/junk 提示、support/admin assist 入口；限制 resend 频率避免滥发。 | 中 | 待实现 | 随 player OTP login 一起实现。 |
| R-019 | “Footer support details every screen.” | 家长在任何步骤遇到问题都能找到帮助，降低中途放弃。 | 所有用户侧页面提供一致 support 信息：support email、office hours phone 或 admin assist。保持克制，不占用主流程空间。 | 中 | 已实现，待测试 OK | 在用户侧主流程页面底部确认 support footer 可见且不遮挡主操作。 |
| R-020 | “Renewal existing mandate: one-click confirm updated amount.” | 续费老用户不应重复填写完整 debit order，减少续费摩擦。 | 如果已有 active mandate，renewal 显示更新后的金额并允许一键确认；需要 audit log 和 confirmation email。 | 中 | 待实现 | 随 renewal portal 和 payment mandate 一起设计。 |
| R-021 | “All forms must be fully responsive and tested on iOS/Android.” | 目标用户主要用手机，移动端不能只是能打开，必须能完成全流程。 | 为核心表单建立 mobile QA 清单和 Playwright mobile viewport smoke tests，覆盖 trial、onboarding、payment、admin assist。 | 高 | 已实现，待测试 OK | Playwright 已覆盖 Desktop Chrome、iPhone 13 viewport、Pixel 5，并在核心业务流中检查横向溢出。 |
| R-022 | “Duplicate passport check by ID number/email before creating new passport.” | 避免同一个孩子/家长重复创建 Player Passport，保护数据质量。 | 创建 passport 前按 ID number、guardian email、player email 查重；命中时引导登录/找回，而不是新建。数据完整性优先级高，虽然 consumer report 里体验优先级偏低。 | 高 | 待实现 | 随 Player master profile 和 OTP login 一起实现。 |
| R-023 | “Trial no-show: fee forfeited; admin can mark absent.” | Trial 现场运营需要明确 no-show 处理，减少退款争议。 | Admin check-in 支持 mark absent/no-show；邮件和政策提示明确 fee forfeited。 | 低 | 待实现 | 随 trial check-in 实现。 |
| R-024 | “One active programme at a time.” | 防止同一 player 同时注册冲突项目，避免付款和运营混乱。 | 注册前检查 active programme；Academy player 不允许直接注册 First Touch / Little Warriors，除非已 withdrawn 或 admin override。 | 高 | 待实现 | 加 programme eligibility service 和测试。 |
| R-025 | “需要在render 环境里实现capetownspurs.co.za email域名的config。” | 所有系统邮件最终应使用 Cape Town Spurs 官方域名发出，提升信任感，并避免用户把邮件当成测试/垃圾邮件。 | 中期方案：Render 环境使用 `EMAIL_FROM=Cape Town Spurs <registrations@capetownspurs.co.za>`；邮件服务仍通过环境变量控制，不把发件人写死在模板里。上线前必须在 Resend/邮件服务完成 `capetownspurs.co.za` 域名验证和 DNS 记录配置。 | 高 | 暂缓，等待官方域名权限 | 拿到 `capetownspurs.co.za` DNS/Resend 权限后，再切回 Resend verified domain，并重新验收 SPF/DKIM/DMARC。 |
| R-026 | “我现在没有办法用capetownspurs.co.za 这个email，我可以config我现有的gmail.com地址吗我需要做到可以发送给其他的register 的email地址。” | 短期先保证 testing/registration flow 能真实发邮件给用户填写的任意邮箱，不被 Resend 测试域限制卡住。 | 新增 `EMAIL_PROVIDER=smtp`，支持 Gmail SMTP：`SMTP_HOST=smtp.gmail.com`、`SMTP_PORT=465`、`SMTP_SECURE=true`、`SMTP_USER`、`SMTP_PASS`。Render 使用 Gmail App Password 作为 secret，不写入代码；后续拿到官方域名权限后可切回 Resend。 | 高 | 已实现，待 Render 配置和邮件测试 OK | 在 Render 配置 Gmail SMTP env vars，触发 trial/admin/onboarding 邮件，确认 Email Logs 为 SENT，registered email 收件箱收到邮件。 |

## 执行计划

### Build registration form 优化

1. 梳理现有流程
   - 确认 trial email、save application、membership record 写入分别在哪些前后端模块。
   - 确认现有数据字段是否已经包含 start date、end date、arrival time、membership code、age group reminder。

2. 邮件与保存逻辑
   - 修改 trial 邮件模板，加入 start date、end date、arrival time。
   - 调整 save application：保存不发送用户邮件。
   - 回归检查正式提交、批准或确认类邮件仍然正常发送。

3. 会员资料写入逻辑
   - 以 membership code 作为判断依据。
   - 已存在：只更新现有 membership record 的 birthday、ageGroup 和 birthday cert file，并更新已有 club invite application。
   - 不存在：创建新 membership record。
   - 其他会员字段保持不变，避免误覆盖已有资料。
   - 避免误删申请历史，会员主档和 application 记录要分清。

4. Age group reminder 视觉强化
   - 把 reminder 放在用户做年龄组相关选择的上下文里。
   - 用更清晰的标题、边框/底色/强调色提升可见性，但避免红色 error 语义。
   - 不增加多余解释，只突出用户必须注意的规则。

5. Date of Birth 输入约束
   - 高亮 Date of Birth 下方的 age group 计算提示。
   - Date of Birth 控件限制可选日期，避免用户选择 Outside Trial Age Groups。
   - disabled input/select 要有明确锁定样式，特别是 club invite lookup 后自动填充的字段。

6. Email composer 富文本化
   - 先确认后端邮件发送是否支持 HTML body，同时保留纯文本 fallback。
   - 前端 admin email composer 从 textarea 改为轻量富文本编辑器。
   - 支持基础格式，不做复杂邮件设计器。
   - 确认 email log 能保存和展示富文本内容，避免丢格式。

## 当前判断

- 最高优先级是 R-002、R-004、R-005：它们直接影响用户沟通和数据正确性。
- R-006 是体验优化，范围应控制在视觉强调和位置优化，不需要扩成新功能。

## 待澄清

暂无。

## 当前改动测试流程

### R-010：Reminder 非错误色样式

1. 打开 `http://localhost:5174/club-registration-platform/`。
2. 进入 `Player Registration`。
3. 保持在 `New Trial` tab。
4. 查看 `Date of Birth` 字段下方的提示区域。
5. 预期结果：
   - `Select a date of birth...` / age group 提示是 amber/yellow 风格，不是红色。
   - `Age group reminder` 是 amber/yellow 风格，不像 error message。
   - 真实错误信息仍然是红色，例如不填必填项后点击 `Save Trial Application`。
6. 切换到 `Club Invite Trial`，输入 membership code 后 lookup。
7. 预期结果：
   - 自动填充并锁定的字段是 blue-gray locked 样式。
   - locked 字段看起来是系统锁定，不像错误状态。

### R-009：Admin Email 富文本 Composer

1. 打开 admin 页面并登录。
2. 在任意有邮件动作的 table 点击 `Send Email` / information check / qualification review。
3. 预期结果：
   - 弹窗中显示富文本编辑区域，不再是普通 textarea。
   - `Bold`、`List`、`Link` 按钮可用。
   - 修改内容后点击 `Send Email`，邮件发送/记录流程正常。

### R-025：Render 官方邮件域名配置

1. 在 Render testing service 确认环境变量：
   - `EMAIL_PROVIDER=resend`
   - `EMAIL_FROM=Cape Town Spurs <registrations@capetownspurs.co.za>`
   - `RESEND_API_KEY` 已配置。
2. 在 Resend 或当前邮件服务后台确认 `capetownspurs.co.za` 已完成域名验证，DNS 记录包含 SPF/DKIM/DMARC 所需配置。
3. 在 Admin UI 触发一封可控测试邮件，例如 resend registration email、qualification review email 或 club invite code email。
4. 预期结果：
   - Backend email log 显示 `SENT`。
   - 收件箱看到发件人为 `Cape Town Spurs <registrations@capetownspurs.co.za>`。
   - 邮件没有进入 spam，邮件 header 没有 SPF/DKIM/DMARC fail。

### R-026：短期 Gmail SMTP 发信配置

1. 在 Google Account 中开启 2-Step Verification。
2. 创建 Gmail App Password，保存给 Render 使用；不要使用 Gmail 登录密码。
3. 在 Render testing service 配置：
   - `EMAIL_PROVIDER=smtp`
   - `EMAIL_FROM=Cape Town Spurs Registration <your-gmail-address@gmail.com>`
   - `SMTP_HOST=smtp.gmail.com`
   - `SMTP_PORT=465`
   - `SMTP_SECURE=true`
   - `SMTP_USER=your-gmail-address@gmail.com`
   - `SMTP_PASS=<Google App Password>`
   - `ADMIN_NOTIFICATION_EMAIL=your-gmail-address@gmail.com` 或实际 admin 收件地址。
4. 重新 deploy Render service。
5. 在用户侧用一个非 Gmail admin 本人的 registered email 提交/触发邮件，例如 trial review successful、club invite code 或 simple registration resend。
6. 预期结果：
   - Admin Email Logs 显示 provider 为 `smtp`，status 为 `SENT`。
   - registered email 收件箱收到邮件。
   - 如果失败，Email Logs 显示 `FAILED` 和 Gmail SMTP 返回的错误信息。

### R-013：First Touch / Little Warriors 年龄不匹配提示

1. 通过 New Trial 或 Club Invite Trial 创建一个有 DOB 的 player，并由 admin 标记 successful。
2. 进入 `Urban Warrior Onboarding`。
3. 输入 authorisation code 和 membership number。
4. 选择与 player 年龄不匹配的 `First Touch` 或 `Little Warriors`。
5. 点击 `Continue`。
6. 预期结果：
   - 页面阻止继续。
   - 错误文案显示当前 season 年龄。
   - 文案说明所选 programme 的允许年龄范围。
   - 文案给出建议 pathway 和 `support@capetownspurs.co.za`。

### R-014：Trial Remaining Spots

1. 打开 `Player Registration > New Trial`。
2. 在 `Date of Birth` 选择一个有效 trial 年龄，例如 Under 12。
3. 预期结果：
   - DOB 下方显示 `Trial places`。
   - 文案展示 “x of y places remaining for Under 12”。
   - 如果某年龄组配置剩余名额为 0，点击 `Save Trial Application` 会阻止提交并提示联系 club。

### R-015：Onboarding Code Email Next Steps

1. 在 admin 里把 New Trial 或 Club Invite Application 标记 `Successful`。
2. 在弹出的 `Final Successful Review Email` composer 中查看默认内容。
3. 预期结果：
   - 邮件默认内容包含 `Next steps:`。
   - 包含打开 registration system、进入 Urban Warrior Onboarding、输入 code 和 membership number、完成 registration/payment。
4. 点击 `Send Email` 后，在 admin email status/log 中确认发送记录正常。

### R-019：Support Footer

1. 打开用户侧任意主流程页面：`Player Registration`、`Urban Warrior Onboarding`、`Holiday Camp` 或 `Meet & Greet`。
2. 滚动到底部。
3. 预期结果：
   - 页面底部显示 `Need help with registration?`。
   - 显示 support email。
   - footer 不遮挡 submit / continue / payment button。

### R-021：Mobile Responsive Smoke

1. 运行 `npm --prefix frontend run test:ui`。
2. 预期结果：
   - Playwright 会在 `chromium`、`mobile-ios-viewport`、`mobile-chrome` 三个项目运行。
   - New Trial → Admin approval → Urban Warrior Onboarding 可在移动端完成。
   - Club Invite Trial lookup/application 可在移动端完成。
   - Holiday Camp membership/payment flow 可在移动端完成。
   - 核心页面不会出现 body-level 横向溢出。
3. 手动补充检查：
   - 在 iOS/Android 真机或浏览器模拟器中确认 date picker、file upload、rich email composer 按钮可点击。

### D-025：Admin Back Office 基础对象列表

1. 打开 admin 页面并登录。
2. 在 Admin dashboard 中查看 summary cards。
3. 预期结果：
   - 能看到 `Players`、`Documents`、`Email Logs` 数量卡片。
   - 能看到 `Players` table，包含 player name、membership、birthday、age group、guardian、status。
   - 能看到 `Documents` table，包含 birth certificate 文件名、类型、关联 player、文件大小，并且 `View` 通过受保护接口打开文件。
   - 能看到 `Email Logs` table，包含 recipient、subject、provider/status、related code、message preview。
4. 发一封 information check 或 qualification review email。
5. 预期结果：
   - 对应 application 的 email status 更新。
   - `Email Logs` table 自动刷新并出现刚发送的记录。

## 已完成需求提炼

### 需求管理与协作流程

| 编号 | 已完成能力 | 业务价值 | 当前状态 |
| --- | --- | --- | --- |
| D-001 | 建立 `需求池.md`，持续记录用户原话、业务目标、技术方案、优先级、状态和下一步。 | 把自然语言需求沉淀成可执行产品判断，避免需求丢失或被过度实现。 | 已完成 |
| D-002 | 建立发布与测试规则：实现后先 dev test，用户确认 OK 后才提交 GitHub / 更新 testing service。 | 避免未确认功能进入 testing，降低返工和误发布风险。 | 已完成 |
| D-003 | 建立代码变更后的测试流程同步规则。 | 每次改动都有可执行验收步骤，方便 dev/testing 环境快速确认。 | 已完成 |

### Trial 邮件与通知

| 编号 | 已完成能力 | 业务价值 | 当前状态 |
| --- | --- | --- | --- |
| D-003 | Trial 相关邮件自动包含 start date、end date、arrival time。 | 家长收到邮件后能直接知道 trial 到场信息，减少人工确认。 | 已实现，待业务测试 OK |
| D-004 | Trial 时间信息用环境变量集中配置，并在后端发送时兜底补齐。 | 后续改 trial 时间不需要改多处代码，也避免 admin 编辑邮件时漏掉关键信息。 | 已实现，待业务测试 OK |
| D-005 | Save application 不再给用户发送 application received 邮件，只保留 admin notification。 | 保存动作回归内部处理语义，避免用户误以为申请已经正式确认。 | 已实现，待业务测试 OK |
| D-006 | Admin 邮件操作支持先编辑再发送，包括 club invite code、information check、qualification review、simple registration resend。 | Admin 可以在发送前检查和微调邮件内容，减少错误沟通。 | 已实现，待业务测试 OK |
| D-007 | Trial information check 和 qualification review 邮件状态在 admin table 中分列展示。 | Admin 能区分信息核查邮件和最终资格结果邮件，减少状态混淆。 | 已实现，待业务测试 OK |

### Club Invite / Membership 数据一致性

| 编号 | 已完成能力 | 业务价值 | 当前状态 |
| --- | --- | --- | --- |
| D-008 | 同一个 membership code 提交 club invite trial 时复用同一个 Player，不新建重复会员。 | 保持会员主档唯一，避免 admin 看到多个同一会员。 | 已实现，待业务测试 OK |
| D-009 | 同 membership code 更新时，只同步 birthday、ageGroup 和最新 birth certificate，其它会员字段不覆盖。 | 修正生日和年龄组，同时保护已有会员资料不被误改。 | 已实现，待业务测试 OK |
| D-010 | ClubInviteApplication 的 membershipCode 已加数据库 unique 约束，并通过 migration 清理历史重复。 | 数据库层面防止重复 application，不只依赖前端或接口逻辑。 | 已实现，待业务测试 OK |
| D-011 | 后端使用 membershipCode upsert ClubInviteApplication，不新增第二条同会员申请。 | 同会员重新提交时更新原申请，符合 admin table 只显示一条记录的预期。 | 已实现，待业务测试 OK |
| D-012 | 前端提交成功后按 id / membership code 替换现有 club invite application state。 | 不刷新页面时也不会临时显示两条重复记录。 | 已实现，待业务测试 OK |

### Admin UI

| 编号 | 已完成能力 | 业务价值 | 当前状态 |
| --- | --- | --- | --- |
| D-013 | Club Invite Applications table 展示 Birthday。 | Admin 可以直接核查生日、birth certificate 和 age group 是否一致。 | 已实现，待业务测试 OK |
| D-014 | New Trial Applications table 展示 Birthday。 | 新 trial 申请审核时生日信息不需要打开详情或文件才能确认。 | 已实现，待业务测试 OK |
| D-015 | Admin table 中 birth certificate 支持查看最新上传文件。 | Admin 能快速验证生日证明，支撑 information check。 | 已实现，待业务测试 OK |

### Registration 表单体验

| 编号 | 已完成能力 | 业务价值 | 当前状态 |
| --- | --- | --- | --- |
| D-016 | Date of Birth 下方新增 age group reminder 高亮块。 | 用户填写生日时能立即注意年龄组规则，降低选错概率。 | 已实现，待业务测试 OK |
| D-017 | Club invite lookup 后自动填充并锁定部分字段。 | 减少用户改错 membership / invite 相关信息。 | 已实现，待业务测试 OK |

### Registration / Onboarding 业务流

| 编号 | 已完成能力 | 业务价值 | 当前状态 |
| --- | --- | --- | --- |
| D-018 | New Trial、Club Invite Trial、Renewal 三个 player registration 入口已整合到主注册流程。 | 用户入口更清晰，trial、邀请试训、续费不会混在一起。 | 已完成 |
| D-019 | New Trial 成功后可进入 Urban Warrior onboarding，并通过 authorisation code 解锁。 | 把 trial 成功结果和正式球员入会流程连接起来。 | 已完成 |
| D-020 | Onboarding 流程计算 trial credit，并在注册费用中抵扣 trial fee。 | 成功 trial 用户不会重复承担已经支付的 trial 成本。 | 已完成 |
| D-021 | Holiday Camp 和 Meet & Greet 已加入 membership/payment flow，能生成 membership code 并模拟付款完成。 | 非 academy player 的活动型注册也能进入可追踪的会员/付款流程。 | 已完成 |
| D-022 | Simple registration 记录在 admin 端展示 payment status、membership code，并支持 resend registration email。 | Admin 可以追踪活动/会员类注册的付款和邮件状态。 | 已完成 |

### Admin 运营工具

| 编号 | 已完成能力 | 业务价值 | 当前状态 |
| --- | --- | --- | --- |
| D-023 | Admin testing data reset 已接入后端和 admin UI。 | 测试阶段可以快速清理数据，降低手工清库成本。 | 已完成 |
| D-024 | Admin 可以生成 club invite membership code 和 trial code。 | 俱乐部邀请试训可以由后台集中生成和管理。 | 已完成 |
| D-025 | Admin 可以查看 club invite trial codes、club invite applications、new trial applications、players、documents、email logs、onboarding records、audit logs。 | 后台覆盖主要运营对象，方便测试和日常核查。 | 已完成 |
| D-026 | Admin document preview 通过受保护接口读取文件。 | Birth certificate 等文件可核查，同时不直接暴露本地/Supabase 存储路径。 | 已完成 |
| D-027 | Admin export CSV 已接入。 | 运营可以导出 trial / onboarding 数据做线下核对。 | 已完成 |

### 技术基础与清理

| 编号 | 已完成能力 | 业务价值 | 当前状态 |
| --- | --- | --- | --- |
| D-028 | 移除未使用的 Mongoose registration controller/model/routes/service。 | 降低维护成本，避免旧 MongoDB 路径和当前 Prisma/Postgres 路径混淆。 | 已完成 |
| D-029 | Render testing deployment 配置包含后端、前端 build、Prisma migration、Postgres database 和关键环境变量。 | testing service 可以通过 Render Blueprint 一体化部署。 | 已完成 |
| D-030 | Email provider、storage provider、payment provider 均保留测试/生产可配置入口。 | 当前可测试，后续接真实 Resend、Supabase、PayFast 时不需要重做架构。 | 已完成 |
| D-031 | 最近的 interaction logic 文档已创建，用于记录业务交互逻辑。 | 把复杂业务流程从代码外显化，方便之后讨论和验收。 | 已完成 |

### 最近 Commit 摘要

| Commit | 日期 | 主题 | 提炼出的能力 |
| --- | --- | --- | --- |
| `b24d81b` | 2026-06-15 | Document interaction logic and email composer request | 补充交互逻辑文档，并记录 email composer 富文本化需求。 |
| `998c6cf` | 2026-06-15 | Add trial email controls and unique club invites | Trial 邮件控制、admin 邮件编辑、club invite membership unique、birthday/ageGroup/birth cert 更新逻辑。 |
| `90c735f` | 2026-06-15 | Add admin testing data reset | Admin 测试数据重置工具。 |
| `2978040` | 2026-06-15 | Update trial and club invite flows | Trial / club invite application、admin review、document upload、authorisation code 等主流程升级。 |
| `a44f575` | 2026-06-14 | Add membership flow for camp and meet greet | Holiday camp 与 meet & greet membership/payment flow。 |
| `8c641e8` | 2026-06-14 | Add admin trial email resend | Admin trial email resend 和邮件状态展示基础能力。 |
| `12e19da` | 2026-06-13 | Remove unused mongoose registration code | 删除旧 Mongoose 注册代码，统一到 Prisma/Postgres。 |
| `1753454` | 2026-06-13 | Update new trial onboarding flow | New trial 到 Urban Warrior onboarding 的流程调整。 |
| `eaa3bb7` | 2026-06-13 | Revert "Stabilise phase 1 frontend MVP" | 回退不适合当前方向的 frontend service 拆分。 |
| `0c9c2d7` | 2026-06-13 | Stabilise phase 1 frontend MVP | 阶段性 frontend MVP 稳定化尝试，后续已按项目方向回退/吸收。 |

### 尚未完成但已进入需求池

| 编号 | 待实现能力 | 业务价值 | 当前状态 |
| --- | --- | --- | --- |
| P-001 | 高亮 Date of Birth 下方 “Select a date of birth...” 提示。 | 让年龄组计算提示更醒目。 | 已实现，待测试 OK |
| P-002 | Date picker 禁用 Outside Trial Age Groups 日期。 | 从输入层阻止无效生日选择。 | 已实现，待测试 OK |
| P-003 | Disabled input/select 使用明确 locked/highlight 样式。 | 让用户知道字段是系统锁定而不是坏掉。 | 已实现，待测试 OK |
| P-004 | Admin email composer 从 textarea 升级为轻量富文本编辑。 | 提升邮件正式度和可读性。 | 已实现，待测试 OK |

## Workflow 文档对照评估

来源：`UPDATED CAPE TOWN SPURS WORKFLOW.docx`。

优先级定义：

- P0：核心身份、登录、支付、数据一致性；不做会阻塞正式业务闭环。
- P1：核心运营 workflow；不做会让流程需要大量人工绕行。
- P2：运营效率、自动化、审核体验；不做仍可测试，但长期会低效。
- P3：体验增强或后续扩展；不阻塞当前 testing。

### 已存在 / 基本已存在

| 编号 | 文档需求 | 当前项目判断 | 覆盖程度 | 优先级 |
| --- | --- | --- | --- | --- |
| W-DONE-001 | 每个 player 有 Cape Town Spurs Player Passport / Member Number。 | `Player.membershipNumber` 已存在，新 trial 和 club invite flow 会生成/复用 membership number。 | 部分符合：字段和生成逻辑存在，但还不是完整“所有首次接触都创建统一 passport”的账号体系。 | P0 |
| W-DONE-002 | Trial / onboarding / renewal code。 | `OneTimeCode` 支持 `TRIAL_AUTHORISATION`、`RENEWAL`；club invite trial code 也已单独建模。 | 部分符合：代码生成存在，expiry 可配置，但 login OTP 未实现。 | P0 |
| W-DONE-003 | Public New Trial registration。 | New Trial form、birth certificate upload、admin list、review success/fail、authorisation code 已存在。 | 部分符合：没有 trial window、basket、bib、真实 PayFast confirmation。 | P0 |
| W-DONE-004 | Club Invitation Trial。 | Admin 生成 membership code / invite code，用户 lookup 后提交 club invite application，无需付款。 | 部分符合：核心 invite flow 已有；Passport login / OTP / venue instructions 尚未完整。 | P0 |
| W-DONE-005 | Admin marks trial successful / unsuccessful and sends outcome email。 | Admin review action、information check email、qualification review email、resend email 已存在。 | 基本符合：邮件内容和富文本仍需业务测试。 | P1 |
| W-DONE-006 | Onboarding after successful trial。 | Authorisation code 可解锁 Urban Warrior onboarding，选择 programme，提交 onboarding record。 | 部分符合：仍缺完整 required fields、真实 payment/debit mandate、incomplete reminder。 | P1 |
| W-DONE-007 | Renewal code。 | Admin 可生成 renewal code / bulk renewal codes。 | 部分符合：没有完整 renewal selection workflow、not renewing message、prefilled renewal portal。 | P1 |
| W-DONE-008 | Mandatory document storage。 | Document model、birth certificate upload、admin preview 已存在。 | 部分符合：文件格式/大小和 onboarding document set 需补齐。 | P1 |
| W-DONE-009 | Admin back office lists。 | Admin UI 已有 trials、club invite applications、players、codes、documents、onboarding、audit logs、CSV export。 | 基本符合：缺 payment history、renewal status、trial scores。 | P2 |
| W-DONE-010 | Holiday camps / meet & greet once-off flow。 | Holiday Camp / Meet & Greet simple registration 已有 membership/payment simulation flow。 | 部分符合：不是完整 dashboard tab + basket + PayFast。 | P2 |
| W-DONE-011 | Centralized Postgres/Prisma data model。 | Player、TrialApplication、ClubInviteApplication、Payment、Document、EmailLog、AuditLog 等已在 Prisma。 | 基本符合：仍需补完整 master profile 字段和历史业务表。 | P0 |

### 需要新做

| 编号 | 文档需求 | 当前缺口 | 建议方案 | 优先级 |
| --- | --- | --- | --- | --- |
| W-NEW-001 | Player / guardian OTP-only login。 | 当前只有 admin login；player 没有 Passport/email + OTP 登录。 | 新增 player auth：输入 membership number 或 email，发送 10 分钟 OTP，验证后进入 player dashboard。 | P0 |
| W-NEW-002 | Player dashboard programme tabs。 | 当前是 public tabs，不是登录后的 player dashboard。 | 登录后展示 Academy Programmes、First Touch、Little Warriors、Holiday Camps 等 tab，并按资格控制入口。 | P0 |
| W-NEW-003 | Player master profile。 | 文档要求 ID number、medical aid、emergency contact、notification opt-ins、age read-only；当前 trial form 字段不足。 | 扩展 Player master profile 和首次注册表单；生日计算 age / ageGroup。 | P0 |
| W-NEW-004 | Trial windows per age group。 | 当前没有按 U12/U14/U16/U18 配置 open date、capacity、registration close。 | 新增 TrialEvent / TrialWindow 表，按年龄组、日期、capacity 控制可注册状态。 | P0 |
| W-NEW-005 | Basket / checkout flow。 | 当前 trial/onboarding/payment 多为模拟或直接 submit。 | 建立 basket / checkout 抽象，trial、programme、camp、direct registration 都走统一 checkout。 | P0 |
| W-NEW-006 | Real PayFast payment confirmation。 | PayFast scaffold 存在，但当前 testing 多为 simulated。 | 完成 PayFast checkout、ITN callback、payment success/failure 状态回写和邮件。 | P0 |
| W-NEW-007 | Bib number + colour assignment。 | 文档要求 trial confirmation 带 bib/color；当前没有 bib model。 | Trial application paid 后按 trial event 自动分配 bib number/color，并展示/邮件通知。 | P1 |
| W-NEW-008 | Trial check-in。 | 文档要求现场 check-in 和 bib 领取；当前没有 check-in 状态。 | Admin/mobile check-in 页面，按 bib 或 membership 查询并标记 attended/no-show。 | P1 |
| W-NEW-009 | Trial assessment app。 | 文档要求最多 20 assessors、按 bib 评分、防重复、总分和 drill-down。 | 单独 assessor app：trial event、bib list、评分表、pass mark、score aggregation。 | P1 |
| W-NEW-010 | Trial refund automation。 | 文档要求 PayFast refund API 和 20% admin fee policy；当前没有 refund。 | 新增 refund request、policy calculation、PayFast refund integration、audit log。 | P1 |
| W-NEW-011 | Renewal selection workflow。 | 当前可生成 renewal code，但没有 end-of-season select invite/not renewing 流程。 | Admin 按 age group/programme 批量标记 invite to renew / not renewing，并发送对应邮件。 | P1 |
| W-NEW-012 | Prefilled renewal portal。 | Renewal code 不能打开完整 prefilled renewal form。 | Renewal portal 读取 Player master profile，允许更新地址、phone、medical、guardian、documents。 | P1 |
| W-NEW-013 | First Touch / Little Warriors direct registration。 | 当前 programme config 里 First Touch / Little Warriors 仍偏 academy onboarding，不符合文档 no trial required。 | 改为 no-trial direct registration，按年龄验证后进入 checkout/onboarding。 | P0 |
| W-NEW-014 | Annual upfront payment with 8% tuition discount。 | 当前 onboarding payment option 有字段，但折扣规则未完整实现。 | 实现 payment option calculation：registration fee 不打折，tuition 年费 8% discount。 | P1 |
| W-NEW-015 | Debit order mandate generation / electronic mandate。 | 当前只是接受 debit order flag/document，未生成 prefilled mandate。 | 生成 debit order form 或接电子 mandate provider，并存储 mandate record。 | P1 |
| W-NEW-016 | Onboarding incomplete reminder / admin assist。 | 文档要求 14 天 offer expiry、7 天前 reminder、Admin please assist。 | 定时任务 + email reminder + admin assist request endpoint。 | P2 |
| W-NEW-017 | Payment history and renewal status。 | Back office 还不能完整查看 player payment history / renewal state。 | 给 Player detail 增加 payments、programmes、renewal status、trial history。 | P2 |
| W-NEW-018 | Automated email matrix。 | 文档列出 login OTP、payment confirmation、onboarding reminder、not renewing 等邮件；当前只覆盖部分。 | 建立 email event matrix 和模板管理，覆盖所有 workflow event。 | P1 |

### 需要优化 / 修正

| 编号 | 文档需求 | 当前问题 | 建议优化 | 优先级 |
| --- | --- | --- | --- | --- |
| W-OPT-001 | Trial email dates/time。 | 当前 trial 日期/arrival time 是全局环境变量，不支持每个 age group/trial window。 | 改为从 TrialWindow / TrialEvent 读取日期、arrival time、venue instructions。 | P0 |
| W-OPT-002 | Age group validation。 | 当前 DOB 计算存在，但需要禁用 Outside Trial Age Groups 日期并强化提示。 | 已进入 R-008；继续业务验收。 | P0 |
| W-OPT-003 | Reminder color。 | 红色 reminder 容易像 error。 | 已进入 R-010；error 才保留红色。 | P2 |
| W-OPT-004 | Birth certificate upload rule。 | 当前 trial birth certificate 只允许 JPEG/PDF、2MB；文档要求 PDF/JPG/PNG、max 5MB。 | 调整前后端 upload validation 和提示文案。 | P1 |
| W-OPT-005 | Email composer。 | 当前富文本能力需要继续确认 HTML email、fallback 和 log 展示。 | 已进入 R-009；继续业务验收。 | P2 |
| W-OPT-006 | Save application language。 | 当前按钮和状态仍可能让用户理解为最终确认。 | 文案区分 Save / Submit / Payment confirmed / Admin verified。 | P2 |
| W-OPT-007 | Club invite profile creation。 | 文档要求 admin 可为无 passport player 创建 minimal profile 并发送 welcome magic link/OTP；当前是生成 invite code。 | Admin invite 时创建/关联 Player master profile，并接入 player OTP login。 | P0 |
| W-OPT-008 | First Touch / Little Warriors programme config。 | 当前 programme flow 与文档 no trial required 有冲突。 | 改 programme rules，直接注册但仍校验年龄。 | P0 |
| W-OPT-009 | Onboarding required fields。 | 文档要求 kit size、sock size、T&C、code of conduct、banking/debit order、documents；当前字段不完整。 | 补齐 onboarding schema、UI 和 validation。 | P1 |
| W-OPT-010 | Master data update strategy。 | 文档要求 player 后续修改地址、电话、medical 会更新 master record；当前只对部分 trial/club invite 字段处理。 | 建立 PlayerProfile update service，明确哪些字段覆盖 master，哪些保留 application snapshot。 | P1 |

### 建议实施顺序

| 阶段 | 目标 | 包含项目 | 理由 |
| --- | --- | --- | --- |
| Phase 1 | 补齐身份和入口基础 | W-NEW-001、W-NEW-002、W-NEW-003、W-NEW-013、W-OPT-008 | 没有 player OTP login 和 dashboard，文档里的多数 workflow 都无法自然发生。 |
| Phase 2 | 补齐 trial 交易闭环 | W-NEW-004、W-NEW-005、W-NEW-006、W-NEW-007、W-OPT-001、W-OPT-002 | Trial 是 academy 主入口，需要 age window、capacity、payment、bib、确认邮件一起闭环。 |
| Phase 3 | 补齐 onboarding / renewal | W-NEW-011、W-NEW-012、W-NEW-014、W-NEW-015、W-OPT-009、W-OPT-010 | Trial 成功后能进入正式 player lifecycle，减少人工处理。 |
| Phase 4 | 运营自动化 | W-NEW-008、W-NEW-009、W-NEW-010、W-NEW-016、W-NEW-017、W-NEW-018 | 这些提升运营效率，但可在核心报名闭环后逐步上线。 |
| Phase 5 | 体验和模板优化 | W-OPT-003、W-OPT-004、W-OPT-005、W-OPT-006 | 不阻塞主流程，但会提升专业度和减少误解。 |

## Consumer Report 对照评估

来源：`Consumer Report.docx`。

这份 report 的价值不在于新增很多功能，而是提醒我们：家长不是技术用户，核心体验必须让他们在手机上清楚知道“我在哪里、为什么不能继续、下一步要做什么、出问题找谁”。因此优先级按转化和支持成本排序。

### 已存在 / 基本已存在

| 编号 | Report 观察 | 当前项目判断 | 需要注意 |
| --- | --- | --- | --- |
| C-DONE-001 | OTP login 对家长来说容易理解。 | 项目尚未完整实现 player OTP login，但 workflow 评估已把它列为 P0。 | 需要把 OTP 做成用户侧主入口，不只是 admin 登录。 |
| C-DONE-002 | Dashboard tabs 清楚。 | 当前 public tabs 已存在，真正登录后的 player dashboard 尚未完成。 | 后续 dashboard 不应做成营销页，要直接服务报名/续费/活动操作。 |
| C-DONE-003 | First Touch / Little Warriors direct registration 方向正确。 | Workflow 已识别当前 programme flow 需要调整为 no-trial direct registration。 | 必须配合年龄校验和付款闭环一起做。 |
| C-DONE-004 | Trial registration + bib email 是清晰主流程。 | Trial application、admin review、邮件基础已存在；bib number/color 尚未实现。 | Bib 需要跟 trial window/capacity/check-in 一起建模。 |
| C-DONE-005 | Refund policy 明确会增强信任。 | 当前没有完整 refund/no-show 自动化。 | 不急着做复杂 refund API，但页面和邮件政策必须先清楚。 |
| C-DONE-006 | “Admin, please assist” 是正确兜底。 | Workflow 已把 admin assist/reminder 作为 P2；当前还未完整实现。 | 对非技术家长很重要，至少核心页面要有清晰 support 入口。 |
| C-DONE-007 | 自动邮件整体方向正确。 | 当前邮件系统、email log、富文本 composer 已有基础。 | 还缺完整 email event matrix，尤其 onboarding code、OTP、payment、renewal。 |

### 需要新做

| 编号 | Report 需求 | 对应需求 | 建议优先级 | 原因 |
| --- | --- | --- | --- | --- |
| C-NEW-001 | Trial registration 显示 remaining spots。 | R-014 / W-NEW-004 | P0 | 直接影响报名信任和付款决策，需要 trial window/capacity 数据支撑。 |
| C-NEW-002 | Onboarding code email 提供明确下一步。 | R-015 / W-NEW-018 | P0 | 用户拿到 code 但不知道怎么用，会直接卡住 onboarding。 |
| C-NEW-003 | 电子 debit order mandate 默认路径。 | R-016 / W-NEW-015 | P1 | 手机用户打印签字上传非常差，会降低正式注册完成率。 |
| C-NEW-004 | OTP resend / spam / contact admin help。 | R-018 / W-NEW-001 | P1 | 登录失败没有恢复路径，会立刻变成人工支持问题。 |
| C-NEW-005 | 全站 support footer / help entry。 | R-019 | P2 | 低成本提升信任，避免用户中途放弃。 |
| C-NEW-006 | Renewal 已有 mandate 时一键确认新金额。 | R-020 / W-NEW-012 | P2 | 减少老用户续费摩擦，但依赖 mandate 和 renewal portal。 |
| C-NEW-007 | Duplicate passport check。 | R-022 / W-NEW-003 | P0 | Consumer report 里体验优先级偏低，但从数据一致性看必须靠前。 |
| C-NEW-008 | Trial no-show absent status。 | R-023 / W-NEW-008 | P2 | 现场运营需要，但可在 trial window/check-in 后做。 |
| C-NEW-009 | One active programme rule。 | R-024 | P1 | 防止用户注册冲突项目，避免付款、名单和运营混乱。 |

### 需要优化 / 修正

| 编号 | Report 需求 | 当前问题 | 对应需求 | 建议优先级 |
| --- | --- | --- | --- | --- |
| C-OPT-001 | First Touch / Little Warriors 年龄错误提示太笼统。 | 用户不知道孩子年龄、允许范围、该选哪个 programme。 | R-013 | P0 |
| C-OPT-002 | 太大不能进 FT/LW、但不确定是否适合 Academy trial 的孩子需要引导。 | 当前 age validation 只阻止，不提供下一步。 | R-013 | P1 |
| C-OPT-003 | Kit / sock size guide。 | 尺码选择没有即时参考。 | R-017 / W-OPT-009 | P2 |
| C-OPT-004 | 所有表单必须移动端可完成。 | 当前有响应式 UI，但没有 iOS/Android 业务路径验收清单。 | R-021 | P0 |
| C-OPT-005 | Onboarding / payment / reminder 邮件需要按真实用户动作检查。 | 邮件能力有基础，但模板完整性未按 consumer journey 验证。 | R-015 / W-NEW-018 | P1 |

### Consumer Report 建议实施顺序

| 阶段 | 目标 | 包含项目 | 判断 |
| --- | --- | --- | --- |
| Consumer Phase 1 | 先让家长不迷路、不重复、不选错 | R-013、R-014、R-015、R-021、R-022 | 这些直接影响报名转化和数据质量，应排在体验装饰前。 |
| Consumer Phase 2 | 补齐登录、付款、支持兜底 | R-016、R-018、R-019、R-024 | 让手机用户能完成闭环，遇到问题能自助或找 support。 |
| Consumer Phase 3 | 提升续费和现场运营效率 | R-017、R-020、R-023 | 重要但依赖前面 player profile、mandate、trial window/check-in 能力。 |

## 业务流程测试需要更新

当前已有 `frontend/tests/business-flow.spec.ts` 和 `backend/scripts/business-flow-test.mjs` 覆盖部分 trial / onboarding / admin review 主路径。根据 Consumer Report，还需要补以下业务验收场景；对应功能未实现前，先记录为测试缺口。

| 测试编号 | 业务场景 | 页面/入口 | 操作步骤 | 预期结果 | 对应需求 |
| --- | --- | --- | --- | --- | --- |
| T-001 | First Touch / Little Warriors 年龄不匹配提示 | Player Registration 或登录后 programme tab | 输入不符合 programme 年龄的 DOB 并尝试继续 | 显示孩子当前年龄、允许年龄范围、推荐 programme 或 contact club，不只显示 generic error | R-013 |
| T-002 | Trial 剩余名额展示 | New Trial registration | 进入 trial form，选择/自动匹配 age group | 页面显示 capacity 与 remaining spots；名额满时不能继续付款或提交 | R-014 |
| T-003 | Onboarding code 邮件下一步说明 | Admin qualification success / email log | Mark trial successful 并发送 onboarding code email | 邮件包含登录、进入 Academy Programmes、Enter Onboarding Code、完成 registration/payment 四步 | R-015 |
| T-004 | 电子 debit order 默认路径 | Onboarding payment / renewal payment | 选择 monthly debit order | 默认进入电子 mandate；print/sign/upload 只作为 fallback；手机 viewport 不要求打印 | R-016 |
| T-005 | Kit / sock size guide | Onboarding form | 点击 kit size / sock size 旁的 guide | 展示清晰尺码对照，不打断已填写表单 | R-017 |
| T-006 | OTP resend 与 support 帮助 | Player login | 请求 OTP、点击 resend、查看帮助提示 | 有 resend rate limit、spam/junk 提示、support/admin assist 入口 | R-018 |
| T-007 | 全站 support footer | 用户侧所有主流程页面 | 在 trial、onboarding、renewal、payment 页面查看底部/帮助入口 | 每个页面都能找到 support email/phone 或 admin assist，且不遮挡主操作 | R-019 |
| T-008 | Renewal 旧 mandate 一键确认 | Renewal portal | 使用已有 active mandate 的 player 登录 renewal | 显示更新后金额并允许 confirm；生成 audit log 和 confirmation email | R-020 |
| T-009 | Mobile responsive smoke | iOS/Android viewport | 跑通 trial、onboarding、payment、admin assist 主路径 | 无横向溢出、按钮可点击、文本不遮挡、上传/日期/富文本/支付控件可用 | R-021 |
| T-010 | Duplicate passport check | First registration / passport creation | 使用已存在 ID number 或 email 再次创建 player | 系统提示已存在并引导登录/找回，不创建重复 Player Passport | R-022 |
| T-011 | Trial no-show | Admin trial check-in | 对已付款 trial application 标记 absent/no-show | 状态更新为 absent/no-show，fee forfeited policy 可见，并写入 audit log | R-023 |
| T-012 | One active programme rule | Programme registration | Academy active player 尝试注册 First Touch / Little Warriors | 被阻止并说明当前 active programme；withdrawn/admin override 才可继续 | R-024 |

## 已完成

- 2026-06-15：创建 `需求池.md`，建立需求记录结构与工作原则。
- 2026-06-15：整理 build registration form session 的 5 个优化点，形成需求列表、优先级和执行计划。
- 2026-06-15：移除 “Need another verification for information in admin” 待做项；该能力已由现有 feature 覆盖，无需新增需求。
- 2026-06-15：明确 R-005 的覆盖范围：membership code 已存在时，只覆盖 birthday 和 birthday cert file，其他会员字段不改。
- 2026-06-15：新增发布与测试规则：实现后先 dev test；如我无法完整测试，必须提示用户测试；只有用户确认 OK 后，才提交 GitHub 并更新 testing service。
- 2026-06-15：实现 R-002、R-004、R-005、R-006；本地静态检查和前端浏览器检查通过，完整 API 测试受本地 PostgreSQL 未运行影响，等待用户测试 OK。
- 2026-06-15：实现 R-007：Admin 的 Club Invite Applications 与 New Trial Applications table 新增 Birthday 列。
- 2026-06-15：补充 R-005：同 membership code 更新 birthday 时，必须同步更新 ageGroup，保持派生信息一致。
- 2026-06-15：补充 R-005：同 membership code 提交 club invite trial 时，更新已有 ClubInviteApplication；Admin list 按 membership code 展示最新一条，避免重复记录显示。
- 2026-06-15：补充 R-005：数据库层面对 ClubInviteApplication.membershipCode 增加 unique 约束；migration 会清理旧重复数据并保留最新记录，本地数据库已成功应用。
- 2026-06-15：补充 R-005：前端提交成功后的 club invite application state 也按 id/membership code 去重替换，避免不刷新页面时显示重复行。
- 2026-06-15：新增 R-008：Date of Birth 提示高亮、禁用 Outside Trial Age Groups 日期、disabled input 高亮样式。
- 2026-06-15：新增 R-009：Admin email template/composer 从纯 textarea 升级为轻量富文本编辑。
- 2026-06-16：新增 R-010：Reminder highlight 不应使用红色，红色容易被理解为 error message；需要改为非错误语义提示色。
- 2026-06-16：实现 R-008 / P-001 至 P-003：DOB 提示高亮、date picker 限制有效年龄范围、disabled 表单字段使用 locked/highlight 样式。
- 2026-06-16：实现 R-009 / P-004：Admin email composer 改为轻量富文本编辑器，支持加粗、列表、链接，并让后端发送 HTML 邮件同时保留纯文本 fallback。
- 2026-06-16：实现 R-010 并调整 R-006 视觉语义：age group reminder 改为 amber/yellow 提示样式，disabled locked 字段改为 blue-gray，红色只保留给 error/action 语义。
- 2026-06-16：新增 R-011：每次改完代码必须同步对应测试流程，包含页面入口、操作步骤、预期结果和人工确认点。
- 2026-06-16：根据 `UPDATED CAPE TOWN SPURS WORKFLOW.docx` 完成项目功能差距评估，按已存在、需要新做、需要优化分类，并给出 P0-P3 优先级和建议实施阶段。
- 2026-06-16：实现已有功能优化 R-013、R-014、R-015、R-019：programme age mismatch 文案、trial remaining spots、onboarding code email next steps、全站 support footer，并同步测试流程。
- 2026-06-16：实现 R-021：Playwright 增加 iPhone 13 与 Pixel 5 mobile projects，核心业务流测试覆盖 mobile viewport，并检查页面无 body-level 横向溢出。
- 2026-06-16：根据 `Consumer Report.docx` 更新需求池和业务流程测试缺口，补充家长视角的高优先级体验、数据一致性和移动端验收项。
- 2026-06-16：补齐 D-025 Admin back office 基础对象列表：前端展示 Players、Documents、Email Logs，后端新增 admin email logs endpoint，邮件发送后自动刷新日志表。
- 2026-06-16：新增并配置 R-025：Render `EMAIL_FROM` 改为 Cape Town Spurs 官方域名发件人；后因暂时没有官方域名权限，改为中期目标，等待 Resend/DNS 验证。
- 2026-06-16：实现 R-026：后端新增 SMTP email provider，Render testing 短期可使用 Gmail SMTP + App Password 给 registered emails 发信。
